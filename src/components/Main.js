import Form from './MainForm';
import Preview from './MainPreview';

const Main = () => {
  return (
    <main className="cards">
      <Preview />
      <Form />
    </main>
  );
};

export default Main;
